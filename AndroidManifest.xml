<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TennisProject"
        tools:targetApi="30">
        <activity

            android:name=".ManagementActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".tossActivity"/>
        <activity android:name=".CountActivity"/>
        <activity android:name=".ExportActivity"/>
        <activity android:name=".PlayerEntryActivity"/>
        <activity android:name=".Server_DTO"/>
        <activity android:name=".PlayerManagement"/>
        <activity android:name=".PlayerEditActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".TournamentEdit"/>
        <activity android:name=".TournamentCreate"/>
        <activity android:name=".TournamentEntry"/>
        <activity android:name=".TournamentEntryList"/>
    </application>
</manifest>